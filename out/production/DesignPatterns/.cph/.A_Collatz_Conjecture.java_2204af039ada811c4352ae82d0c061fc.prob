{"name":"A. Collatz Conjecture","group":"Codeforces - Codeforces Round 1047 (Div. 3)","url":"https://codeforces.com/contest/2137/problem/A","interactive":false,"memoryLimit":256,"timeLimit":1000,"tests":[{"id":1757255820736,"input":"3\n1 4\n1 5\n5 4\n","output":"1\n10\n21 public static void  main(String[] args) {\n    \n        // how take input from user in java\n        // we use Scanner class to take input from user\n        // Scanner class is present in java.util package\n        // we need to import Scanner class to use it\n        // import java.util.Scanner;\n\n        Scanner sc = new Scanner(System.in);\n\n        // scanner.next() → Reads a single word (stops at space).\n        // scanner.nextLine() → Reads the full line, including spaces.\n\n        System.out.println(\"Enter your first  name: \");\n        String firstName = sc.next();\n        System.out.println(\"Enter your last  name: \");\n        String lastName = sc.next();\n        System.out.println(\"Your entered name : \" + firstName + \" \" + lastName);    \n\n// sc.next() reads a word but leaves the newline (\\n) in the input buffer.\n// sc.nextLine() reads until a newline, so it picks up the leftover \\n from next(), making it seem like it skipped input.\n        sc.nextLine(); // to clear the buffer\n\n        //  take input using nextLine() method\n        System.out.println(\"Enter your full name: \");\n        String fullName = sc.nextLine();\n        System.out.println(\"Your entered name : \" + fullName);    \n        sc.close();\n\n\n        \n\n    } public static void  main(String[] args) {\n    \n        // how take input from user in java\n        // we use Scanner class to take input from user\n        // Scanner class is present in java.util package\n        // we need to import Scanner class to use it\n        // import java.util.Scanner;\n\n        Scanner sc = new Scanner(System.in);\n\n        // scanner.next() → Reads a single word (stops at space).\n        // scanner.nextLine() → Reads the full line, including spaces.\n\n        System.out.println(\"Enter your first  name: \");\n        String firstName = sc.next();\n        System.out.println(\"Enter your last  name: \");\n        String lastName = sc.next();\n        System.out.println(\"Your entered name : \" + firstName + \" \" + lastName);    \n\n// sc.next() reads a word but leaves the newline (\\n) in the input buffer.\n// sc.nextLine() reads until a newline, so it picks up the leftover \\n from next(), making it seem like it skipped input.\n        sc.nextLine(); // to clear the buffer\n\n        //  take input using nextLine() method\n        System.out.println(\"Enter your full name: \");\n        String fullName = sc.nextLine();\n        System.out.println(\"Your entered name : \" + fullName);    \n        sc.close();\n\n\n        \n\n    }\n"}],"testType":"single","input":{"type":"stdin"},"output":{"type":"stdout"},"languages":{"java":{"mainClass":"Main","taskClass":"ACollatzConjecture"}},"batch":{"id":"6c8408d6-538e-4669-a9af-965ef00166ab","size":1},"srcPath":"/Users/rakes/Desktop/Learning/BackendLearning/JavaCode/DesignPatterns/A_Collatz_Conjecture.java"}